const fs=require('node:fs'),path=require('node:path');
module.exports=function(markdown){const source=fs.readFileSync(path.join(__dirname,'../EXTERIOR-PRODUCT-RESEARCH.md'),'utf8');const parts=source.split(/^## /m);const intro=parts.shift();const sections=parts.map((part,i)=>{const split=part.indexOf('\n');return{id:'topic-'+(i+1),title:part.slice(0,split),body:part.slice(split+1)};});return `<section class="section research-page"><div class="scope-banner"><strong>EXTERIOR ONLY</strong><span>Interiors, furniture and indoor finishes will be planned later.</span></div>${markdown(intro)}<p><a class="button" href="/guides/overview.html">New: visual guides, quantity calculators & purchasing notes →</a></p><div class="research-overview"><img src="/outputs/elevations/selected-a/front-approved.png" width="1536" height="1024" alt="Approved ivory exterior used as the research reference"><div><span class="eyebrow">FROM THE IMAGE TO REAL MATERIALS</span><h2>Keep the character.<br>Specify every layer.</h2><p>Start with smooth ivory plaster, restrained profiles and dark metalwork. Compare complete installed systems before choosing brands.</p><a href="/exterior.html">Compare the exterior views ↗</a><br><a href="/downloads/EXTERIOR-PRODUCT-RESEARCH.md" download>Download the full research ↓</a></div></div><div class="research-actions"><label for="research-search">Find a product or topic<input id="research-search" type="search" placeholder="Try windows, waterproofing, price…"></label><button id="expand-research">Expand all</button><button id="collapse-research">Collapse all</button></div><p id="research-count" aria-live="polite">18 research topics</p><nav class="topic-links" aria-label="Research topics">${sections.map(s=>`<a href="#${s.id}">${s.title}</a>`).join('')}</nav><div id="research-topics">${sections.map((s,i)=>`<details class="research-topic" id="${s.id}"${i<2?' open':''}><summary>${s.title}</summary><div class="topic-body">${markdown(s.body)}</div></details>`).join('')}</div><p id="research-empty" hidden>No matching topics. Try a brand name or a broader word.</p><p class="fine">Prices and source details checked 8 September 2026. No product order or supplier enquiry has been placed.</p></section>`;};
